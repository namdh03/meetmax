import Header from "./components/Header";

const MessageBox = () => {
    return (
        <div className="messages__box">
            <Header />
        </div>
    );
};

export default MessageBox;
