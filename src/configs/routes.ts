import { RouteConfigType } from "@/types";

const routes: RouteConfigType = {
    home: "/",
    signIn: "/sign-in",
    signUp: "/sign-up",
    messages: "/messages",
    notFound: "*",
};

export default routes;
